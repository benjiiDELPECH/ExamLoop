# Local development configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/examloop
spring.datasource.username=${USER}
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.flyway.enabled=true

# Disable AI for local testing (use mock)
spring.ai.openai.api-key=sk-test-mock
examloop.ai.enabled=false

# Stripe test mode
stripe.api.key=sk_test_mock
stripe.webhook.secret=whsec_mock
stripe.price.id=price_mock

server.port=8080
