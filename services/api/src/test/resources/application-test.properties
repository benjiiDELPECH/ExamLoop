# Test configuration
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true

# Disable AI for tests
examloop.ai.mock=true

# Disable Stripe for tests
stripe.api-key=
stripe.webhook-secret=
